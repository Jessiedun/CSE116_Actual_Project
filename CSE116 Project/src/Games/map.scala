package Games

object map {
  var startingLocation = new gridLocation(0, 9)
  var gridx: Int = 25
  var gridy: Int = 9
  def niceMap(newMap: Int): gridLocation = {
    startingLocation
  }
}

class map {
  var gridx: Int =25
  var gridy: Int = 9
  var startingLocation = new gridLocation(0, 9)
}